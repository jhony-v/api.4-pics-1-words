import socket from "socket.io";

/**
 * Create new room when a user create a shared game
 */
export function EventCreateRoomOnline(pubsub: socket.Socket): void {}

/**
 * Chat between users in the room
 */
export function EventChatInRoom(pubsub: socket.Socket): void {

}
